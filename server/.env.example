PORT=3001
ANTHROPIC_API_KEY=your_anthropic_api_key_here
JWT_SECRET=your_jwt_secret_here   # REQUIRED — generate with: openssl rand -hex 32
NODE_ENV=development               # set to "production" in deploy config
ALLOWED_ORIGIN=http://localhost:5173
